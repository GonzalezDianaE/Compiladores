program
    begin
        integer inc (integer x)
            begin
                return x + 1;
            end
    integer inc (integer x)
        begin
            return (x+1);
        end
    bool isZero (integer x, integer y)
        begin
            return (x+y);
        end
    {integer inc (integer x, integer y)
        begin
            return (x+1);
        end}
    void  main()
        begin
            integer y;
            y=4;
            if (y == 1) then
                {ddd


                }
                begin{dadsdas}
                    return 1;
                    {}
                end
            else
                begin
                    return inc (y);
                end
        end
end